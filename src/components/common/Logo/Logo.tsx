// External deps
import React, { FC } from 'react';

// Internal deps
import Icon from "components/ui/Icon";

const Logo: FC = () => {
    return (
        <Icon icon="logo" width={226} height={40} />
    );
};

export default Logo;
